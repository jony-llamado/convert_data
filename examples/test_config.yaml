# Forge conversion configuration
# Generated from: sample_data/robomimic_ph/can_ph_image/1.0.1
# Source format: rlds

# Target format for conversion
target_format: lerobot-v3

# fps: 30  # Specify FPS (required if not in source)
# robot_type: franka  # Specify robot type

# Camera name mapping (source → target)
# Modify target names as needed for your use case
cameras:
  robot0_eye_in_hand_image: robot0_eye_in_hand_image
  agentview_image: agentview_image

# Field mapping (source → target)
# Uncomment and modify as needed
fields:
  # action: steps/action  # Specify action field path
  # state: observation/robot_state  # Specify state field path

# Video encoding settings (optional)
# video:
#   codec: h264
#   crf: 23  # Quality (lower = better, 18-28 typical)

# Behavior settings
fail_on_error: false
skip_existing: true
